---
  retries: 3
- name: Install required packages
  become: yes
  apt:
    name: "{{ query('flattened', [['aptitude'], '{{ packages }}']) }}"
    state: present

- name: "Do apt {{ apt_upgrade }} upgrade"
  become: yes
  apt:
    update_cache: yes
    upgrade: "{{ apt_upgrade }}"
